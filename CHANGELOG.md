# Changelog

این پروژه از **Semantic Versioning** پیروی می‌کنه: شماره‌ی نسخه به شکل `MAJOR.MINOR.PATCH` هست (مثلاً `1.1.0`) و طبق این قانون بالا می‌ره:

- **MAJOR** (عدد اول) وقتی زیاد می‌شه که یه تغییر اساسی بدیم که با نسخه‌ی قبل سازگار نیست (مثلاً ساختار دیتابیس عوض بشه و اتاق‌های قدیمی دیگه کار نکنن).
- **MINOR** (عدد وسط) وقتی زیاد می‌شه که یه قابلیت جدید اضافه بشه ولی چیزی که قبلاً کار می‌کرد رو خراب نکنه.
- **PATCH** (عدد آخر) وقتی زیاد می‌شه که فقط یه باگ رفع بشه، بدون قابلیت جدید.

شماره‌ی نسخه‌ی فعلی توی فایل `common.js` (متغیر `APP_VERSION`) نگه‌داری می‌شه و گوشه‌ی سایت (پایین اسم Together) نمایش داده می‌شه.

## [1.2.0]

### تغییر کرد
- درصد پیشرفتِ هر ویدیو دیگه توی مرورگر تنها ذخیره نمی‌شه؛ توی یه جدول Supabase مشترک نگه داشته می‌شه، پس هر دو نفر همون درصد رو می‌بینن (هرکدوم بیشتر دیده باشه، همون درصد نمایش داده می‌شه).
- برای این قابلیت، یه جدول جدید به اسم `progress` لازمه که SQL ساختش تو README اضافه شده.

## [1.1.0] - نسخه‌ی فعلی

### اضافه شد
- نمایش شماره‌ی نسخه‌ی سایت.
- درصد پیشرفت جلوی هر ویدیو تو صفحه‌ی اول، که با دیدن ویدیو خودش پر می‌شه (پیشرفت روی همون مرورگری که باهاش دیدی ذخیره می‌شه).

## [1.0.0]

### اضافه شد
- ساخت و ورود به اتاق با کد.
- سینک لحظه‌ای پلی/پاز/سیک بین دو نفر با Supabase Realtime.
- رابط کاربری کامل انگلیسی با انیمیشن‌های ورود، نبض زنده بودن دوست، و درخشش لحظه‌ی سینک‌شدن.
